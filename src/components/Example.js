import React from 'react';



// let getName = function (p) {
//     return new Promise((resolve, reject) => {
//         if (p.hasOwnProperty('name')) {
//             resolve(p.name);
//         }else {
//             reject(p.age);
//         }
//     });
// }
//
// getName({name: 'Sun', age: 18})
//     .then(name => console.log(name))
//     .catch(age => console.log(age));
//

const Example = () => {
  return (
    <div>
      Example
    </div>
  );
};

Example.propTypes = {
};

export default Example;
